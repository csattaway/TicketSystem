@model IEnumerable<TicketSystem.Models.tlkpUserType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.txtUserType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dtmCreate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.txtUserType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dtmCreate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idsUserType }) |
            @Html.ActionLink("Details", "Details", new { id=item.idsUserType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idsUserType })
        </td>
    </tr>
}

</table>
