@model IEnumerable<TicketSystem.Models.tlkpSeverityType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.txtSeverityType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dtmCreate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.txtSeverityType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dtmCreate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idsSeverityType }) |
            @Html.ActionLink("Details", "Details", new { id=item.idsSeverityType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idsSeverityType })
        </td>
    </tr>
}

</table>
